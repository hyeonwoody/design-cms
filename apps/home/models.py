"""
Wagtail page types live here.

Skeleton: no page models yet. Stage 04 (core-api) adds a HomePage(Page) that hosts
the tabbed UI. Defining it later keeps the initial migration minimal.
"""
# from wagtail.models import Page
#
# class HomePage(Page):
#     pass
